## Hackatown 2019

Commands:

```sh
# build
docker-compose build
# run
docker-compose up
```

#### Env vars

server
```dotenv 
AWS_DNS=<mongo on ec2>
```

ui
```dotenv
REACT_APP_API_KEY=<maps api-key>
```
